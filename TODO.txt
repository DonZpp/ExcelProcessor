完成课表生成代码
    // 个人课程统计代码
    //   完成教师名单读取
    //   据教师名单统计课程
    //   将统计结果输出

    课表生成
        // 修改逻辑，在增加课程名称到课程信息的映射时，同步增加教师名称到课程信息的映射，两都最终应指向同一对象
        // 读取教师及所教课程
            // 创建初始数据存储结构
            // 读取初始数据
                // 读取Ban列表
        // 根据班级生成key为数字索引的字典
        完成ArrangeCurr函数